# dotfiles

nvim + tmux configs.

```text
nvim/
  lua/
    config/
    core/
    plugins/
tmux/
```

nvim uses lazy.nvim for plugins.
tmux config is plain (.psmux.conf), no manager.

no setup. copy what you need.
